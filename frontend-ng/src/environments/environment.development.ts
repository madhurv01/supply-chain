export const environment = {
  production: false,
  // Backend port not finalized yet — one-line fix here once the C# backend's
  // launch settings are confirmed.
  apiBaseUrl: 'http://localhost:5000',
};
